import { MayoToastProps } from "./MayoToast.types";
import { useEffect, useRef } from "react";

export function MayoToast ({ open, type, position = "top-right" }: MayoToastProps){
    // const mayoOpener = useRef<HTML>
    return (
        
    )
};
